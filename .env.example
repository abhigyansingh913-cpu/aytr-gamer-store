# ---- AYT R STORE — environment configuration ----
# Copy this file to `.env.local` and fill in your values.

# Comma-separated emails allowed into the admin panel.
# The password is NEVER stored here — it lives in Firebase Authentication.
VITE_ADMIN_EMAILS=admin@yourdomain.com
